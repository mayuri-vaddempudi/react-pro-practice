import styles from "./footer.module.css"

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <div>
                <p className={styles.content}>My footer</p>
            </div>
        </footer>
    )
}
export default Footer;